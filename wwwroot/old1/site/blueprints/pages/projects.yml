title: Projects
files:
  template: project


columns:
 - width: 2/2
  sections:
       mypagetable:
        type: pagetable
        headline: Projects Index
        templates: project
        columns:
          project:
             label: Project
             text: "{{ page.title }}"
             type: text
              width: 1/2
          media:
             label: Media
             text: "{{ page.tags }}"
             type: text
             width: 1/6
          client:
             label: Client
             type: text
             text: "{{ page.client }}"
             width: 1/4
          year:
             label: Year
             type: text
             text: "{{ page.year }}"
             width: 1/6

